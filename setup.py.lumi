from setuptools import setup

setup(
    name="hmsc",
    version="0.1.0",
    author="[removed for review]",
    license='GPLv3+',
    install_requires=[
        'pyreadr',
        'tensorflow-probability==0.20.0',
        'ujson',
    ]
)
